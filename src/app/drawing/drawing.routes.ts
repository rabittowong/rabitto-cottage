import { Routes } from '@angular/router';

import { DrawingContentComponent } from './drawing-content.component';

export const routes: Routes = [
  { path: '', component: DrawingContentComponent },
];
